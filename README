INVGEN is an invariant generation tool that parses programs (limited
to a single loop) and produces a TPTP file that can be use


To build the executable you will need to have Make, Bison and Flex
installed. From the main directory, simply run

$ make

to build the executable 'invgen'

optionally followed by

$ make clean