set(QUIT_PARSER_SOURCES
    GclParser.cpp
    GclScanner.cpp
    GclParsingContext.cpp
)
set(QUIT_PARSER_HEADERS
    GclParser.hpp
    GclParsingContext.hpp
)

add_library(parser ${QUIT_PARSER_SOURCES} ${QUIT_PARSER_HEADERS})
target_include_directories(parser PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
target_link_libraries(parser program)

# suppress clang-warnings related to code, which is autogenerated by FLEX
target_compile_options(
    parser PRIVATE -Wno-deprecated-register
)
