int [] a, b;
int i, alength, blength, k;

requires 0 <= alength;
requires 0 <= blength;

requires blength <= alength;
requires k <= blength;
requires i == 0;
ensures forall int i, 0 <= i & i < k ==> a[i] == b[i];

while (i < k) do
:: true -> a[i] = b[i]; i = i + 1;
od