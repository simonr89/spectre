int [] a, b;
int i, alength, blength;

requires 0 <= alength;
requires 0 <= blength;

requires blength <= alength;
requires i == 0;
ensures forall int j, i==blength & 0 <= j & j < blength ==> a[j] == b[j];

while (i < blength) do
:: true -> a[i] = b[i]; i = i + 1;
od