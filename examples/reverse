int [] a, b;
int i, alength, blength;

requires blength == alength;
requires i == 0;
ensures forall int i, 0 <= i & i < alength ==> a[i] == b[alength - 1 - i];

while (i < alength) do
:: true -> a[i] = b[alength - 1 - i]; i = i + 1;
od