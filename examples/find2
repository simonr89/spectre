int [] a;
int i, alength, v;

requires i == 0;
requires alength >= 0;
ensures forall int j, 0 <= j & j <= alength & j < i ==> a[j] != v;

while (i < alength & a[i] != v) do
:: true      -> i = i + 1;
od